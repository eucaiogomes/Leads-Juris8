{
  "name": "juris8-det-leads-admin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API e painel administrativo simples para os leads da LP Juris8 DET.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
